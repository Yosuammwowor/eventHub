<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Hub</title>
  </head>
  <body>
    <h1>Edit Event</h1>
    <form action="/events/<%= events._id %>/edit?_method=PUT" method="post">
      <label for="title">Title:</label>
      <input type="text" name="title" id="title" value="<%= events.title %>" />
      <label for="description">Description:</label>
      <input
        type="text"
        name="description"
        id="description"
        value="<%= events.description %>"
      />
      <label for="location">Location:</label>
      <input
        type="text"
        name="location"
        id="location"
        value="<%= events.location %>"
      />
      <label for="date">Date:</label>
      <input type="date" name="date" id="date" value="<%=
      events.date.getFullYear() %>-<% if ((events.date.getMonth() +
      1).toString().length < 2) { %><%= "0" + (events.date.getMonth() +
      1).toString() %><% } else { %><%= events.date.getMonth() + 1 %><% } %>-<%
      if ((events.date.getDate()).toString().length < 2) { %><%= "0" +
      (events.date.getDate()).toString() %><% } else { %><%=
      events.date.getDate() %><% } %>" />
      <button type="submit">Edit</button>
    </form>
  </body>
</html>
